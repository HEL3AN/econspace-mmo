# Единый перевод строк в репозитории: текст хранится с LF, при выгрузке
# на Windows git сам подставит CRLF. Убирает предупреждения «LF will be
# replaced by CRLF» на коммитах.
* text=auto eol=lf

# Бинарные файлы — без нормализации.
*.png binary
*.ttf binary
